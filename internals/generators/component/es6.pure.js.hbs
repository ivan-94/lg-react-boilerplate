/**
*
* {{ properCase name }}
* @flow
*/

import React from 'react';
// import styled from 'styled-components';

{{#if wantMessages}}
import { FormattedMessage } from 'react-intl';
import messages from './messages';
{{/if}}

type PropsType = {
}

class {{ properCase name }} extends React.PureComponent<void, PropsType, void> { // eslint-disable-line react/prefer-stateless-function
  render () {
    return (
      <div>
      {{#if wantMessages}}
        <FormattedMessage {...messages.header} />
      {{/if}}
      </div>
    );
  }
}

export default {{ properCase name }};
